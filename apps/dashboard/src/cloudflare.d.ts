// Provided by the Cloudflare runtime at deploy time; the catch-all route reads
// the service binding and the publish token from it.
declare module "cloudflare:workers" {
  export const env: unknown
}
